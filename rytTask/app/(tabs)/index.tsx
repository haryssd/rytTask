import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const index = () => {
  return (
    <SafeAreaView className="flex-1">
      <View className="flex">
        <Text className="text-5xl text-blue-600">Testis</Text>
      </View>
    </SafeAreaView>
  );
};

export default index;
